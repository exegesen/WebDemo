@model WebDemo.Models.Person
<div class="row">
    <div class="col-sm">@Model.Name</div>
    <div class="col-sm">@Model.City</div> 
    <div class="col-sm"><input type="submit" value="Remove"></div> 
</div>
